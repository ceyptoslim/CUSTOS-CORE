apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-custos
spec:
  selector:
    app: {{ .Release.Name }}-custos
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
  type: {{ .Values.service.type }}
